export default function Home({testUserChange}) {

    return (
      <div>
        <h1>Hello from home-page!</h1>
        <button onClick={testUserChange}>login-test</button>        
      </div>
    );
}
